<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="cardView" parent="TextAppearance.AppCompat.Medium">
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/largeMargin</item>
        <item name="android:layout_marginRight">@dimen/largeMargin</item>
        <item name="android:layout_marginTop">@dimen/smallMargin</item>
        <item name="android:layout_marginBottom">@dimen/largeMargin</item>
    </style>

    <style name="contentText" parent="TextAppearance.AppCompat.Medium">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/mediumMargin</item>
        <item name="android:textSize">@dimen/mediumText</item>
        <item name="android:textColor">@color/primary_text</item>
    </style>

    <style name="propertyText" parent="contentText">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="nameText" parent="propertyText">
        <item name="android:textAllCaps">true</item>
        <item name="android:textSize">@dimen/largeText</item>
    </style>

</resources>
